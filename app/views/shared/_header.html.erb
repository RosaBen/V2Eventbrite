<nav>
    <div class="wrapper">
        <ul class="mainMenu">
          <li class="item"><%= link_to "Accueil", root_path , class: "btn"%></li>
            <% if user_signed_in? %>
              <li class="item" id="account"><%= link_to "🧑 Mon profil", user_path(current_user), class: "btn" %>
              </li>
              <li class="item" id="all_events"><%= link_to "🗞️ Tous les evenements", events_path, class: "btn" %></li>
              <li class="item" id="my_events">
                <a href="#my_events" class="btn">📅 Mes évènements</a>
                <ul class="subMenu">
                  <% current_user.created_events.each do |event| %>
                    <li><%= link_to event.title, guests_event_path(event)%>
                    </li>
                  <% end %>
                </ul>
              </li>
              <li class="item"><%= button_to "Se déconnecter",destroy_user_session_path, 
            method: :delete, data: { confirm: "Es-tu sûr(e) ?" } , class: "btn"%></li>
            <% else %>
              <li class="item" id="connexion">
                <a href="#connexion" class="btn"></i>Connexion</a>
                <ul class="subMenu">
                              <li>
            <%= link_to "Se connecter", 
            new_user_session_path%>
          </li>
                        <li>
            <%= link_to "S'inscrire", 
            new_user_registration_path %>
          </li>
                </ul>
            </li>
            <% end %>
        </ul>
    </div>
</nav>



